{
  "scripts": {
    "build": "echo \"no build script; skipping\"",
    "test": "node --test tests",
    "test:unit": "node --test tests/unit",
    "test:integration": "node --test tests/integration",
    "coverage": "c8 --reporter=lcov --reporter=text-summary --reporter=json-summary node --test tests/unit tests/integration"
  }
}
